#Application Name
spring:
  application:
    name: mediapp-patients #This is the name for the remote configuration file
  cloud:
    config:
      uri: http://172.17.0.2:8888 #where the config-service is running
      fail-fast: true